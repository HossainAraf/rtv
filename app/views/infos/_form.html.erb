<h2>Pay</h2>
<%= form_with(model: info) do |form| %>
  <% if info.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(info.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
        <% info.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :card, style: "display: block" %>
    <%= form.text_field :card %>
  </div>

  <div>
    <%= form.label :cvv, style: "display: block" %>
    <%= form.text_field :cvv %>
  </div>

  <div>
    <%= form.label :expired, style: "display: block" %>
    <%= form.text_field :expired %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
